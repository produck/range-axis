import * as Structure from './Structure/index.mjs';

export const operator = (_a, _b) => {
	const a = Structure.Axis.normalize(_a);
	const b = Structure.Axis.normalize(_b);
	const result = [];

	return result;
};
